# [ubuntu_initramfs_recovery_guide](https://github.com/aeronje/ubuntu_initramfs_recovery_guide/blob/main/recovery.md)
After a sudden power loss or filesystem corruption, Ubuntu may fail to mount the root partition and drop into the initramfs recovery shell instead of proceeding to the login screen.
